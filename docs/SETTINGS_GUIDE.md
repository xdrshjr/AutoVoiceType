# AutoVoiceType - 设置窗口使用指南

## 快速导航

### 打开设置窗口

1. **右键点击托盘图标** → 选择"⚙️ 设置"
2. **双击托盘图标**（可选）
3. **首次运行**自动打开

### 页面说明

| 页面 | 功能 | 必配项 |
|------|------|--------|
| ⚙️ 基础设置 | API密钥、语言、日志 | ✅ API密钥 |
| 🎤 音频设置 | 麦克风、采样率 | - |
| ⌨️ 输入设置 | 输入方式、延迟 | - |
| 🔧 高级设置 | 标点、超时、重置 | - |
| ℹ️ 关于 | 版本、使用说明 | - |

---

## 基础设置

### API密钥配置（必须）

1. 访问 [阿里云DashScope控制台](https://dashscope.aliyun.com)
2. 注册/登录账号
3. 创建API密钥
4. 复制密钥到"API密钥"输入框
5. 点击"验证"按钮测试格式
6. 点击"保存"

**提示**:
- 密钥默认隐藏显示，点击"显示"按钮可查看
- 密钥长度通常>20字符
- 保管好密钥，不要泄露

### 识别模型选择

- `fun-asr-realtime`: 通用语音识别（推荐）
- `paraformer-realtime-v1`: 高精度识别

**建议**: 新用户使用默认模型即可

### 通用设置

- **界面语言**: 简体中文 / English（暂未国际化）
- **日志级别**: 
  - `INFO`: 普通用户推荐
  - `DEBUG`: 开发调试使用
  - `WARNING`: 仅警告和错误
  - `ERROR`: 仅错误信息
- **开机自启**: 勾选后程序随系统启动

---

## 音频设置

### 麦克风选择

1. 点击下拉框查看可用麦克风
2. 选择"默认设备"或指定设备
3. 点击"刷新"重新检测设备

**问题排查**:
- 看不到麦克风？检查驱动和权限
- 设备列表空白？点击"刷新"按钮
- 识别失败？尝试切换其他麦克风

### 音频参数

**采样率**:
- `16000 Hz`: 标准语音识别（推荐）
- `8000 Hz`: 低质量，节省带宽
- `44100/48000 Hz`: 高保真，通常无需使用

**声道数**:
- `单声道 (1)`: 语音识别标准（推荐）
- `立体声 (2)`: 音乐录制使用

**缓冲大小**:
- `3200 字节`: 默认值
- 更大缓冲：延迟高，稳定性好
- 更小缓冲：延迟低，可能丢包

**建议**: 保持默认值，除非遇到音频问题

---

## 输入设置

### 输入方法

**剪贴板方案（推荐）**:
- ✅ 兼容性最好（95%+应用）
- ✅ 速度快
- ⚠️ 会临时覆盖剪贴板
- 💡 自动恢复原内容（可配置）

**Win32 API**:
- ✅ 使用Windows原生API
- ⚠️ 中文支持有限
- 💡 适合纯英文输入

**PyAutoGUI**:
- ✅ 兼容性极高（99%）
- ⚠️ 速度最慢
- 💡 其他方法失败时自动降级

### 输入参数

**字符延迟** (0.01-1.0秒):
- 影响Win32/PyAutoGUI方法
- 默认: 0.05秒
- 问题场景：部分应用输入过快会丢字

**粘贴延迟** (0.01-1.0秒):
- 影响剪贴板方案
- 默认: 0.1秒
- 粘贴前后的等待时间

**最大长度** (100-50000字符):
- 限制单次输入文本长度
- 默认: 10000字符
- 超长文本会被截断

**自动恢复剪贴板**:
- ✅ 勾选：输入后恢复原剪贴板内容
- ❌ 取消：不恢复，保留识别结果

---

## 高级设置

### 识别设置

**智能标点符号**:
- ✅ 启用：自动添加标点符号
- ❌ 禁用：不添加标点
- 💡 依赖API支持，效果可能有限

**超时时间** (5-120秒):
- 识别请求超时时间
- 默认: 30秒
- 网络较差时可适当增加

### 配置文件管理

**配置路径**: `~/.autovoicetype/config.json`

**操作**:
- **打开配置文件**: 使用记事本直接编辑
- **打开配置目录**: 查看日志和备份
- **重置配置**: 恢复所有设置为默认值（谨慎操作）

**警告**: 直接编辑JSON文件需要注意格式，建议使用界面修改

---

## 关于页面

### 版本信息

- 应用版本号
- 构建日期
- Python版本

### 使用说明

```
1. 按住【右Ctrl键】开始语音输入
2. 对着麦克风说话
3. 释放【右Ctrl键】结束输入
4. 识别结果将自动输入到当前窗口的光标位置
```

### 相关链接

- [阿里云DashScope控制台](https://dashscope.aliyun.com)
- [DashScope API文档](https://help.aliyun.com/zh/dashscope/)

---

## 配置保存

### 按钮说明

| 按钮 | 功能 | 窗口 |
|------|------|------|
| 取消 | 放弃修改，关闭窗口 | 关闭 |
| 应用 | 保存配置，继续编辑 | 保持打开 |
| 保存 | 保存配置并关闭 | 关闭 |

### 状态提示

- 修改配置后，底部显示"有 N 项配置待保存"
- 保存成功后显示"✓ 配置已保存"
- 2秒后状态文本自动消失

### 未保存提醒

关闭窗口时，如有未保存的配置：
- 弹出确认对话框
- 选择"是"放弃修改并关闭
- 选择"否"返回继续编辑

---

## 首次运行向导

### 触发条件

- API密钥未配置
- 或配置文件为空

### 向导流程

1. 显示欢迎对话框
2. 说明功能和配置要求
3. 提供DashScope控制台链接
4. 点击"确定"打开设置窗口
5. 配置API密钥后保存
6. 重新启动应用

---

## 常见问题

### Q1: 找不到托盘图标？

**A**: 检查系统托盘设置，确保AutoVoiceType图标显示。

### Q2: 无法保存配置？

**A**: 
1. 检查文件权限（`~/.autovoicetype/`目录）
2. 确保配置格式正确
3. 查看日志文件排查错误

### Q3: API密钥验证失败？

**A**:
1. 确认密钥复制完整（无空格）
2. 检查密钥长度（>10字符）
3. 登录控制台确认密钥有效

### Q4: 麦克风列表为空？

**A**:
1. 检查麦克风是否连接
2. 点击"刷新"按钮
3. 检查麦克风驱动
4. 查看系统隐私设置（麦克风权限）

### Q5: 配置修改后不生效？

**A**:
1. 确认点击了"应用"或"保存"
2. 部分配置需重启应用
3. 检查托盘通知提示

### Q6: 如何备份配置？

**A**:
1. 高级设置 → 打开配置目录
2. 复制 `config.json` 文件
3. 保存到安全位置

### Q7: 误删配置如何恢复？

**A**:
1. 如有备份，直接替换
2. 如无备份，重新配置
3. 或使用"重置配置"功能恢复默认值

---

## 快捷操作

### 键盘快捷键

- `Alt + S`: 切换到基础设置（可能需要设置支持）
- `Alt + A`: 切换到音频设置
- `Alt + I`: 切换到输入设置
- `Esc`: 取消（关闭窗口）

### 鼠标操作

- **单击导航项**: 切换页面
- **双击托盘图标**: 打开设置（可选）
- **右键托盘图标**: 显示菜单

---

## 开发者信息

### 配置文件结构

```json
{
  "api": {
    "dashscope_api_key": "",
    "base_websocket_url": "wss://dashscope.aliyuncs.com/api-ws/v1/inference",
    "model": "fun-asr-realtime"
  },
  "audio": {
    "sample_rate": 16000,
    "channels": 1,
    "chunk_size": 3200,
    "format": "pcm"
  },
  "input": {
    "preferred_method": "clipboard",
    "input_delay": 0.05,
    "paste_delay": 0.1,
    "restore_clipboard": true,
    "max_input_length": 10000
  },
  "general": {
    "auto_start": false,
    "language": "zh-CN",
    "log_level": "INFO"
  },
  "recognition": {
    "semantic_punctuation_enabled": false,
    "timeout": 30
  }
}
```

### 扩展配置页面

1. 创建新页面类继承 `BasePage`
2. 实现 `load_config(config)` 方法
3. 在 `SettingsWindow._create_pages()` 中添加
4. 在侧边栏导航中添加项

### 样式定制

编辑 `assets/styles.qss` 文件：
- 修改颜色：搜索 `#34a853`（绿色）
- 修改圆角：搜索 `border-radius`
- 修改间距：搜索 `padding` 和 `margin`

---

## 技术支持

- **文档**: 查看 `docs/` 目录
- **日志**: `~/.autovoicetype/logs/`
- **配置**: `~/.autovoicetype/config.json`

---

**最后更新**: 2025-12-28  
**版本**: 1.0.0

